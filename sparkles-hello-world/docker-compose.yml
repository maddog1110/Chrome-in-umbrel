FROM zenika/alpine-chrome:latest

# Set environment variables
ENV DISPLAY=:99
ENV VNC_PASSWORD=password

# Expose VNC port
EXPOSE 5900

# Command to start Chromium with VNC
CMD ["--no-sandbox", "--remote-debugging-port=9222"]
